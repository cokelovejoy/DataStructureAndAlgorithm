function deepClone(parent) {
    // 1. 判断传入的值的类型 如果是基本类型的数据，直接返回
    if (typeof parent != "object") {
        return parent
    }
    // 2. 判断数组，对象，Date对象， RegExp对象，function分别处理
    const type = Object.prototype.toString.call(parent)

    if (type === "[object Array]") {

    }
    if (type === "[object Array]") {

    }
    if (type === "[object Array]") {

    }
    if (type === "[object Array]") {

    }
}